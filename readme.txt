README FILE

1.) To compile the source code, go to the terminal and then use the commands “gcc visualiser.c -o visualiser”.
2.) To run the executable use the command “./visualiser”.
3.) The code starts executing. Now the user needs to provide the relevant options to continue using the code.
4.) The program will run in command prompt.

